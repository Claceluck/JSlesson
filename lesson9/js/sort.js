let users = [
    {
        login: "qwe",
        age: 23,
        city: "Москва",
    },
    {
        login: "asd",
        age: 23,
        city: "тверь",
    },
    {
        login: "rty56",
        age: 58,
        city: "Москва",
    }

];

function sortArr (first, second) {

    return first.city.localeCompare(second.city);
}
// someArr.sort(sortArr);
// console.log(someArr);

function sortUsers (first, second){
    let res;
    if (first.city.localeCompare(second.city) === 0){
     }
    if (first.age.localeCompare(second.age) === 0) {
     } else 
    if ( first.login.toLowerCase().localeCompare(second.login.toLowerCase()) )
}
    console.log()
}
